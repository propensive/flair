# `B5` — Bracket inner spacing

**Principle:** B — Balance

Raised by flair when there is unwanted space
immediately inside a single-line bracket pair.

## Cause

In single-line constructs, no space is permitted directly after an
opening bracket or directly before a closing bracket.

## Example messages

```
[↯B5] no space is permitted directly after `(`
```

```
[↯B5] no space is permitted directly before `)`
```

## Resolution

Remove the space adjacent to the bracket; multi-line forms use a
different rule (see `B4`).

## See also

- [The standard](../consequent-style.md)
- Source: `src/plugin/flair.BalanceRules.scala`
